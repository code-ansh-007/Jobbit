//  ?  to start the expo development server in you phone without being connected to the same wifi you can run the command -->

// ! npx expo start --tunnel

// ? but be sure to install expo-cli globally other wise it wont work

// ! to publish or update your app use the command

// * npx expo publish
